'use client';

import Layout from '@/components/layout/Layout';
import ChatPanel from '@/components/chat/ChatPanel';

const Chat = () => (
  <Layout>
    <ChatPanel />
  </Layout>
);

export default Chat;
